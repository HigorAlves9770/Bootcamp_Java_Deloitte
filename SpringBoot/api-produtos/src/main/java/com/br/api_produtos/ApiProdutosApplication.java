package com.br.api_produtos;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Inicializa a aplicação - classe principal responsável pela inicialização do código.
// Starts the application - main class responsible for initializing the application.
@SpringBootApplication
public class ApiProdutosApplication {

    public static void main(String[] args) {
        SpringApplication.run(ApiProdutosApplication.class, args);
    }
}
